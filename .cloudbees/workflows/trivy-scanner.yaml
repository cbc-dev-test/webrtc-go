apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: trivy-scanner
on:
  workflow_dispatch: {}
permissions:
  scm-token-own: read
  scm-token-org: read
  id-token: write
jobs:
  scan-and-evaluate:
    steps:
      - name: checkout-source-code
        uses: cloudbees-io/checkout@v2
        with:
          path: "webrtc-repo"

      - name: Run Trivy Scan
        id: trivy-step
        uses: cloudbees-io/trivy-plugin@v1
        with:
            workspace-dir: "webrtc-repo"

      - id: print-gosec-outputs
        name: Print gosec outputs
        uses: docker://alpine:latest
        run: |
          echo "Trivy outputs:"
          echo "Critical: ${{ steps.trivy-step.outputs.critical-count }}"
          echo "Very high: ${{ steps.trivy-step.outputs.very-high-count }}"
          echo "High: ${{ steps.trivy-step.outputs.high-count }}"
          echo "Medium: ${{ steps.trivy-step.outputs.medium-count }}"
          echo "Low: ${{ steps.trivy-step.outputs.low-count }}"
          echo "Policy Subject: ${{ steps.trivy-step.outputs.policy-subject }}"
          echo "CLOUDBEES_OUTPUTS directory: $CLOUDBEES_OUTPUTS"

      # Printing policy subject details for testing purpose
      - id: print-policy-subject
        name: Print Policy subject
        uses: docker://alpine:latest
        run: |

          echo "=== Files in /cloudbees/workspace/policy-store ==="                                                                                                                              
          find "/cloudbees/workspace/policy-store" -type f | while read file; do                                                                                                               
            echo ""                                                                                                                                                               
            echo "File: $file"                                                                                                                                                    
            cat "$file"                                                                                                                                                           
            echo "---"                                                                                                                                                            
          done