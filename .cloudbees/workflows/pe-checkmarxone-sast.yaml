apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: checkmarxone-scanner
on:
  workflow_dispatch: {}
permissions:
  scm-token-own: read
  scm-token-org: read
  id-token: write
jobs:
  scan-and-evaluate:
    steps:
      - name: checkout-source-code
        uses: cloudbees-io/checkout@v2
        with:
          path: "webrtc-repo"

      - name: Run Gosec Scan
        id: gosec-step
        uses: cloudbees-io/checkmarxone-sast-plugin@v1
        with:
            workspace-dir: "webrtc-repo"

      - id: print-checkmarxone-outputs
        name: Print checkmarxone outputs
        uses: docker://alpine:latest
        run: |
          echo "Gosec outputs:"
          echo "Critical: ${{ steps.checkmarxone-step.outputs.critical-count }}"
          echo "Very high: ${{ steps.checkmarxone-step.outputs.very-high-count }}"
          echo "High: ${{ steps.checkmarxone-step.outputs.high-count }}"
          echo "Medium: ${{ steps.checkmarxone-step.outputs.medium-count }}"
          echo "Low: ${{ steps.checkmarxone-step.outputs.low-count }}"
          echo "Policy Subject: ${{ steps.checkmarxone-step.outputs.policy-subject }}"
          echo "CLOUDBEES_OUTPUTS directory: $CLOUDBEES_OUTPUTS"

      # Printing policy subject details for testing purpose
      - id: print-policy-subject
        name: Print Policy subject
        uses: docker://alpine:latest
        run: |

          echo "=== Files in /cloudbees/workspace/policy-store ==="                                                                                                                              
          find "/cloudbees/workspace/policy-store" -type f | while read file; do                                                                                                               
            echo ""                                                                                                                                                               
            echo "File: $file"                                                                                                                                                    
            cat "$file"                                                                                                                                                           
            echo "---"                                                                                                                                                            
          done